body {
  background-color: rgb(var(--bg-color));
  background-image: linear-gradient(
      to right,
      rgba(var(--text-color)) 1.2px,
      transparent 1px
    ),
    linear-gradient(to bottom, rgba(var(--text-color)) 1.2px, transparent 1px);
  background-size: 60px 60px;
  background-position: center -2px;
  color: rgb(var(--text-color));
  position: relative;

  &::after {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: -1;
    background-image: linear-gradient(
      165deg,
      rgba(var(--bg-color), 0.9),
      rgba(var(--bg-color)) 70%,
      rgb(var(--bg-color))
    );
  }
}
